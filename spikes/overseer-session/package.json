{
  "name": "curia-spike-overseer-session",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Spike #83: overseer session prototype — SDK loop, one query() per Discord message, resume per thread",
  "scripts": {
    "start": "node overseer.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.0",
    "discord.js": "^14.16.0",
    "zod": "^4.0.0"
  }
}
