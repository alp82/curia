{
  "name": "curia-daemon",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Curia service: agent-facing MCP surface + Discord bridge + durable escalation record",
  "//engines": "The Node pin (#357, applied by #409). A warning and not a wall: npm prints it and installs anyway, because a daemon that refuses to install is worse than the stack trace it prevents. The pin that binds is the image, in deploy/compose.yaml and config/curia.yaml.",
  "engines": {
    "node": ">=24.19.0"
  },
  "scripts": {
    "start": "node src/index.mjs",
    "test": "node --test 'test/*.test.mjs'",
    "check-pr-title": "node bin/check-pr-title.mjs",
    "build-attach-index": "node bin/build-attach-index.mjs",
    "build-agent-image": "node bin/build-agent-image.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.220",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "discord.js": "^14.16.0",
    "yaml": "^2.6.0",
    "zod": "^4.4.3"
  }
}
