{
  "name": "curia-daemon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Curia daemon: worker-facing MCP surface + Discord bridge + durable escalation record",
  "scripts": {
    "start": "node src/index.mjs",
    "test": "node --test 'test/*.test.mjs'",
    "build-attach-index": "node bin/build-attach-index.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.220",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "discord.js": "^14.16.0",
    "yaml": "^2.6.0",
    "zod": "^4.4.3"
  }
}
